<script>
 // To make up for long loading times
(() => {
  const maxLoadTime = 5000; // 5 seconds threshold
  const startTime = performance.timing.navigationStart;
  let messageElem = null;

  // Check elapsed loading time every 500ms
  const intervalId = setInterval(() => {
    const elapsed = Date.now() - startTime;
    if (elapsed > maxLoadTime && !messageElem) {
      messageElem = document.createElement('div');
      Object.assign(messageElem.style, {
        position: 'fixed',
        top: '0',
        left: '0',
        width: '100%',
        padding: '1em',
        backgroundColor: '#f8d7da',
        color: '#721c24',
        fontSize: '1.2rem',
        textAlign: 'center',
        zIndex: '9999',
      });
      messageElem.textContent = "It seems you're having internet troubles. Refresh the website to remove this alert. ERR_SL_INT10";
      document.body.appendChild(messageElem);

      clearInterval(intervalId); // stop further checks once message is shown
    }
  }, 200);

  // REMOVED: When the page fully loads, remove the message if it was shown
</script>
<script>
(() => {
  const oldDomainPrefix = "sc-web-blls.onrender.com";
  if (window.location.hostname.startsWith(oldDomainPrefix)) {
    const messageElem = document.createElement('div');
    Object.assign(messageElem.style, {
      position: 'fixed',
      top: '0',
      left: '0',
      width: '100%',
      padding: '1em',
      backgroundColor: '#f8d7da',
      color: '#721c24',
      fontSize: '0.7rem',
      textAlign: 'center',
      zIndex: '9999',
    });
    messageElem.textContent = "You are using the render version. This version will not be updated until December 1. Only use it to login then go back to legacy.";
    document.body.appendChild(messageElem);
  }
})();
</script>